import React from 'react';
import {Links, Meta} from "@remix-run/react";

const Head: React.FC = () => {
    return (
        <head>
            <Meta/>
            <Links/>
        </head>
    );
};

export default Head;
